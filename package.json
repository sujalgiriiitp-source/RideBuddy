{
  "name": "ridebuddy-monorepo",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "npm --prefix server install",
    "start": "npm --prefix server start"
  }
}
